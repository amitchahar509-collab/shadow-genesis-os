# V8_COMPLETION_QUEUE — Shadow Genesis OS

> Ranked by impact. Created 2026-07-09 (V8 loop, Phase 0). Extend existing systems — never rebuild, never duplicate.

| # | Gap (V8) | Impact | Depends | Implementation plan | Test method | Completion proof |
|---|----------|--------|---------|---------------------|-------------|------------------|
| G0 | ~~**Integrated autonomous pipeline**~~ — ✅ DELIVERED cycle 8. `createCompany()` chains DISCOVER→AEGIS→VENTURE→CUSTOMER→BOARD→BUILD gate; `VentureRun` record; reuses `Company`. | 10 | all gates ✓ | ✅ `pipeline/company.ts`; `/api/genesis/company` | ✅ 5 tests: NO_GO halts, strong passes, honesty labels, autonomous discovery, honest failure | ✅ Acceptance run RUN-000011: no idea → discovered → gates scored → board CONDITIONAL 59% → **7/7 BUILT** → company + VENTURE_RUN.md |
| G1 | **World Scanner upgrade** (Gap 1) — add frequency/urgency/who-suffers to OPPORTUNITY output → WORLD_PROBLEM_GRAPH. | 7 | G0 | extend OPPORTUNITY prompt+fallback+model fields | scan → graph has urgency/sufferers | graph artifact fields populated |
| G2 | ~~**Approval Control Center**~~ (Gap 8) — ✅ DELIVERED cycle 9. `ApprovalRequest` queue + transparent risk scoring + api-tool enforcement (external HTTP writes block); approvals single-use; pending expires 24h. | 8 | — | ✅ `approvals/` module + `/api/genesis/approvals` (GET/POST/PATCH) + api-tool gate | ✅ 8 tests | ✅ e2e: blocked (APR-000001, risk 45) → human approve → executes once → replay blocked, status EXECUTED |
| G3 | **Demand Graph + Customer Match** (Gap 2) — problem↔user↔industry↔product mapping; DEMAND_MATCH_SCORE. | 7 | G0 | ProductDNA/DemandMatch models + MATCH agent | product → ranked matches | match rows + artifact |
| G4 | ~~**Acquisition loop**~~ (Gap 4) — ✅ DELIVERED cycle 11. ACQUISITION agent runs the ladder PRICING→AUDIENCE→CHANNEL over extended `GrowthExperiment` (additive fields: experimentId/subject/kind/dataSource/learning/approvalId). Simulated measurements labelled SIMULATION; CHANNEL = real action → G2 approval queue, never fabricates. Wired into the operator's weekly loop. | 7 | G2 ✓ (G3 deferred) | ✅ `agents/v8-acquisition.ts` + `/api/genesis/acquisition` | ✅ 7 tests | ✅ e2e: operated mission — wk1 PRICING ($154 wins), wk2 AUDIENCE (Healthcare), wk3 CHANNEL→APR gate, approve → AWAITING_EXECUTION, 0 fabricated results |
| G5 | ~~**Long-Horizon Operator**~~ (Gap 5) — ✅ DELIVERED cycle 10. Tick-driven (restart-safe) 30/60/90-day missions: DAILY real-metric reviews (failures → QUALITY tasks), WEEKLY venture re-checks (drift flags), MONTHLY board decision (SCALE/PIVOT/KILL/DOUBLE_DOWN; KILL pauses the company), FINAL at horizon. Pipeline hands BUILT companies to a 30-day mission automatically. | 7 | G0 ✓ | ✅ `operator/` + `LongMission`/`OperatorReview` + `/api/genesis/operator` (tick/tickAll/pause/resume) | ✅ 8 tests (simulated time recorded as asOf) | ✅ e2e: company from nothing → BUILT → LM-000002 → 10 reviews over 30 simulated days → monthly PIVOT → COMPLETED |
| G6 | **Agent Arena** (Gap 6) — Alpha/Beta/Gamma parallel solutions; judge picks winner. | 6 | G0 | arena runner over existing agents + judge | 3 runs → 1 winner | ArenaRun row + verdict |
| G7 | **Agent Evolution completion** (Gap 7) — auto-retire/create from AgentMetric trends. | 6 | — | extend improvement/analyzer | weak workflow retired | evolution log row |
| G8 | **App Marketplace** (Gap 3) — Product DNA registry + demand matching for user apps. | 5 | G3 | marketplace models + API | app → matches | marketplace rows |
| G9 | ~~**Reality Feedback Brain**~~ (Gap 10) — ✅ DELIVERED cycle 14. Products POST REAL telemetry → ingest reacts: ERROR→QUALITY task, neg FEEDBACK→ENGINEERING, FEATURE_REQUEST→GROWTH, USAGE/RETENTION→real GrowthMetric, CONVERSION→completes acquisition CHANNEL experiment with dataSource=REAL. Every signal→memory. Reuses/extends `RealitySignal`. | 6 | G4 ✓ | ✅ `reality-feedback/` + `/api/genesis/feedback` + Mission Control panel | ✅ 7 tests | ✅ full-loop e2e: channel AWAITING_EXECUTION → product reports 37/920 → LEARNED/REAL (4%); ERROR→T-112 CRITICAL |
| G10 | **SaaS layer** (Gap 9) — auth/orgs/roles/limits/audit. | 8 | — | NextAuth + models + route guards | unauthenticated → 401 | 401 test green |
| G11 | **Plugin/Skill marketplace** (Gap 11) — custom agents/tools registry (AgentTemplate/CustomTool exist — extend). | 5 | — | performance tracking on existing models | template perf rows | tracked rows |
| G12 | ~~**Benchmark Arena**~~ (Gap 12) — ✅ DELIVERED cycle 13. Real-execution scored suite measuring DISCRIMINATION (rank strong>weak; refuse unsupported confidence) across EVIDENCE/VENTURE/CUSTOMER/BOARD/CHAIN + optional BUILD; autonomy score, success rate, real ms/tokens, mode; persisted for trend. Dashboard panel with RUN button. | 6 | G0 ✓ | ✅ `benchmark/` + `BenchmarkRun` + `/api/genesis/benchmark` + panel | ✅ 5 tests | ✅ live run: autonomy 100/100, 5/5 passed, HEURISTIC, 2689ms (venture 79>21, customer 82.5%>11%, board GO/NO_GO, chain all gates) |
| G13 | ~~**Dashboard UI**~~ — ✅ DELIVERED cycle 12. Two new tabs in the live dashboard: **Venture Intelligence** (pipeline runs table + AEGIS claims + venture analyses + customer sims + board decisions, honesty chips surfaced, CREATE COMPANY button) and **Mission Control** (interactive approval queue with approve/reject, long-mission progress + TICK ALL, acquisition experiment memory). | 6 | G0 ✓ | ✅ `sections/venture-intelligence.tsx` + `sections/mission-control.tsx` wired into `genesis-dashboard.tsx` | tsc/lint/build + SSR check | ✅ SSR renders both tabs; live data verified on all 7 APIs; approve-button path exercised (APR-000001 → APPROVED by dashboard-operator); TICK ALL path exercised |

## Cycle 8 — DELIVERED: G0 (Integrated Autonomous Pipeline)

- `pipeline/company.ts`: `createCompany()` — DISCOVER (OPPORTUNITY, optional focus / existing opportunityId) → VENTURE (asserts AEGIS market claim) → CUSTOMER simulation → AEGIS subject aggregate → BOARDROOM debate over the full quantified context → **build gate** (MVP build via `dispatchGoal` only on GO/CONDITIONAL; NO_GO halts honestly). `VentureRun` model records every stage; `Company` row created on approval; `VENTURE_RUN.md` artifact; memory record; API `GET/POST /api/genesis/company`.
- **Acceptance test passed (real execution)**: RUN-000011, *no idea given* → discovered OPP-000002 → venture 58 WATCH · truth 7% · customer reality 72 (60% buy of 200 SIMULATED personas) · board CONDITIONAL 59% with condition “no evidence base” surfaced → full build **7/7 done** → company `co-opp-000002`. Artifact banners HEURISTIC mode + NO_WEB_EVIDENCE + SIMULATION labels throughout.
- **Bug found & fixed by this cycle's tests**: `count()+1` id allocation in VENTURE (`VC-`), CUSTOMER (`SIM-`), OPPORTUNITY (`OPP-`) collided after any row deletion (P2002). Replaced with numeric max-scan (same fix as EX-/CLM- ids). Full suite now stable across repeated runs.
- Verified: tsc ✓, eslint ✓, **70/70 tests ×3 runs** (5 new), build compiles `/api/genesis/company`.

## Re-audit after cycle 14 (next, ranked)

The outer learning loop is closed: deployed products teach Genesis (errors→fix tasks, requests→backlog, usage→real metrics, conversions→REAL experiment data), and the two honesty boundaries the Acquisition Engine deliberately left open are now crossable with genuine external data — nothing is fabricated. The full directive pipeline now runs end-to-end AND feeds back. What remains is production-hardening + breadth, not missing loop legs. Ranked: (1) **G10 SaaS/auth** — the top production blocker; every `/api/genesis/*` route is open, including the human-authority approval endpoints and the write-heavy feedback/company endpoints; (2) **G1 World Scanner upgrade** (only matters with a browser key — discovery is the weakest gate); (3) **G6 Agent Arena** (Alpha/Beta/Gamma + judge); (4) **G7 evolution completion** (auto-retire/create from AgentMetric trends); (5) **G3 demand graph / G8 marketplace**. Also outstanding: exercise the LLM path (one key lifts every heuristic gate + the benchmark score), and the Next-16/Downloads-junction dev+preview limitation (env debt, documented).

## Re-audit after cycle 13 (superseded)

Genesis now measures itself: the arena proves the intelligence stack discriminates correctly (100/100 in heuristic mode) and gives a trend line that will rise when an LLM key lands — the last *self-measurement* gap is closed. What remains are capability additions, not missing feedback. Ranked: (1) **G9 Reality Feedback** — the highest-leverage remaining: it unlocks REAL experiment data + channel execution (the two honesty boundaries the acquisition engine waits on) and closes the outer learning loop; (2) **G10 SaaS/auth** — still the top production blocker (every route open); (3) **G1 World Scanner upgrade** (needs a browser key); (4) **G6 Agent Arena** / **G7 evolution completion** / **G3 demand graph** / **G8 marketplace**. Environment debt: Next-16 dev + the preview browser can't run through the Downloads junction — UI is verified via a real-path dev server + data-path exercise, not browser pixels.

## Re-audit after cycle 12 (superseded by cycle 13)

"No hidden systems" is closed: every intelligence layer (pipeline, AEGIS, venture, customer sim, boardroom, approvals, operator, acquisition) is visible in the dashboard with its honesty labels, and the human-authority actions (approve/reject, tick) are one click. Honest boundaries unchanged: CHANNEL execution + REAL measurements wait on G9; heuristic reasoning waits on an LLM key. Environment note: Next 16 dev breaks when launched via the Downloads NTFS junction (doubled-path ENOENT) — run dev from `D:\shadow-os-dev\project` directly. Remaining ranked: (1) **G12 Benchmark Arena** — Genesis still doesn't score itself (last self-measurement gap); (2) **G9 Reality Feedback** — unlocks REAL experiment data + channel execution; (3) **G1 World Scanner upgrade** (needs a browser key); (4) **G10 SaaS layer** (auth is still the top production blocker); (5) **G6 Agent Arena / G7 evolution completion**.
