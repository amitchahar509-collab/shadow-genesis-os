# V8_COMPLETION_QUEUE — Shadow Genesis OS

> Ranked by impact. Created 2026-07-09 (V8 loop, Phase 0). Extend existing systems — never rebuild, never duplicate.

| # | Gap (V8) | Impact | Depends | Implementation plan | Test method | Completion proof |
|---|----------|--------|---------|---------------------|-------------|------------------|
| G0 | ~~**Integrated autonomous pipeline**~~ — ✅ DELIVERED cycle 8. `createCompany()` chains DISCOVER→AEGIS→VENTURE→CUSTOMER→BOARD→BUILD gate; `VentureRun` record; reuses `Company`. | 10 | all gates ✓ | ✅ `pipeline/company.ts`; `/api/genesis/company` | ✅ 5 tests: NO_GO halts, strong passes, honesty labels, autonomous discovery, honest failure | ✅ Acceptance run RUN-000011: no idea → discovered → gates scored → board CONDITIONAL 59% → **7/7 BUILT** → company + VENTURE_RUN.md |
| G1 | ~~**World Scanner Engine**~~ (Gap 1) — ✅ DELIVERED cycle 20. Discovers problems from Genesis's OWN accumulated real intelligence — reality-feedback signals (G9), marketplace demand gaps (G8), failed ventures — with frequency/urgency/who-suffers/alternatives/opportunity-score, AEGIS-verified; promotes into the build pipeline. Web scanning activates with a search key (honest NO_WEB label otherwise). | 7 | G8/G9 ✓ | ✅ `world-scanner/` + `WorldProblem` + `/api/genesis/world` + dashboard panel | ✅ 6 tests | ✅ e2e: real error×4 → WP opp 87 → promote → OPP → VENTURE 68 + CUSTOMER 64% (World→AEGIS→Venture→Customer) |
| G2 | ~~**Approval Control Center**~~ (Gap 8) — ✅ DELIVERED cycle 9. `ApprovalRequest` queue + transparent risk scoring + api-tool enforcement (external HTTP writes block); approvals single-use; pending expires 24h. | 8 | — | ✅ `approvals/` module + `/api/genesis/approvals` (GET/POST/PATCH) + api-tool gate | ✅ 8 tests | ✅ e2e: blocked (APR-000001, risk 45) → human approve → executes once → replay blocked, status EXECUTED |
| G3 | ~~**Demand Graph + Customer Match**~~ (Gap 2/3) — ✅ DELIVERED cycle 18. Product DNA fingerprint (problem/category/features/targetUsers/alternatives/keywords) + Customer Match: ranks demand segments by market fit (adoption from the REAL seeded CUSTOMER sim, category↔industry affinity heuristic) → DEMAND_MATCH_SCORE + who/where(community)/why-now/urgency/adoption; projects product↔problem↔industry into the KnowledgeNode/KnowledgeEdge graph. | 7 | G0 ✓ | ✅ `demand/` + `ProductDNA/DemandMatch` + `/api/genesis/demand` + dashboard panel | ✅ 7 tests | ✅ e2e: fintech product → DNA Fintech → demand 58, top Finance HIGH (affinity-lifted), 4 graph edges |
| G4 | ~~**Acquisition loop**~~ (Gap 4) — ✅ DELIVERED cycle 11. ACQUISITION agent runs the ladder PRICING→AUDIENCE→CHANNEL over extended `GrowthExperiment` (additive fields: experimentId/subject/kind/dataSource/learning/approvalId). Simulated measurements labelled SIMULATION; CHANNEL = real action → G2 approval queue, never fabricates. Wired into the operator's weekly loop. | 7 | G2 ✓ (G3 deferred) | ✅ `agents/v8-acquisition.ts` + `/api/genesis/acquisition` | ✅ 7 tests | ✅ e2e: operated mission — wk1 PRICING ($154 wins), wk2 AUDIENCE (Healthcare), wk3 CHANNEL→APR gate, approve → AWAITING_EXECUTION, 0 fabricated results |
| G5 | ~~**Long-Horizon Operator**~~ (Gap 5) — ✅ DELIVERED cycle 10. Tick-driven (restart-safe) 30/60/90-day missions: DAILY real-metric reviews (failures → QUALITY tasks), WEEKLY venture re-checks (drift flags), MONTHLY board decision (SCALE/PIVOT/KILL/DOUBLE_DOWN; KILL pauses the company), FINAL at horizon. Pipeline hands BUILT companies to a 30-day mission automatically. | 7 | G0 ✓ | ✅ `operator/` + `LongMission`/`OperatorReview` + `/api/genesis/operator` (tick/tickAll/pause/resume) | ✅ 8 tests (simulated time recorded as asOf) | ✅ e2e: company from nothing → BUILT → LM-000002 → 10 reviews over 30 simulated days → monthly PIVOT → COMPLETED |
| G6 | ~~**Agent Arena**~~ (Gap 6) — ✅ DELIVERED cycle 16. ALPHA/BETA/GAMMA each make a distinct strategic bet on the same mission (transparent param transforms), scored by the REAL stack (VENTURE + CUSTOMER + AEGIS); judge weights 7 dimensions → winner = argmax(score), never hardcoded; board reviews the winner; winning pattern + failed strategies → memory. | 6 | G0 ✓ | ✅ `arena/` + `ArenaCompetition/ArenaEntry` + `/api/genesis/arena` (ADMIN-gated) + dashboard panel | ✅ 6 tests incl. winner=argmax + input-driven | ✅ acceptance "best SaaS opportunity": ALPHA(v71/c77 but feas35)/BETA/GAMMA scored → BETA wins 55, board reviews winner |
| G7 | ~~**Agent Evolution completion**~~ (Gap 7) — ✅ DELIVERED cycle 17. Engine reads real AgentMetric + recurring FailureAnalysis per agent → decides one action: NO_ACTION (insufficient data / healthy), IMPROVE_PROMPT (append corrective guard via prompt-versioning), RETIRE_WORKFLOW (rollback active prompt), CREATE_SPECIALIST (propose AgentTemplate). Data-driven, dry-run-capable, audited. | 6 | metrics/prompts/failures ✓ | ✅ `evolution/` + `EvolutionAction` + `/api/genesis/evolution` (ADMIN) + dashboard panel | ✅ 8 tests (every branch on seeded real data) | ✅ e2e: improve→prompt v2 guard, retire→rollback v1, specialist→template, evolveAll dry sweep 12 agents 0 applied |
| G8 | ~~**App Marketplace**~~ (Gap 3) — ✅ DELIVERED cycle 19. Apps register with Product DNA + auto demand match; bidirectional matching (app→demand and problem→apps via keyword/category relevance gate); marketplace intelligence = category coverage + demand GAPS (unserved segments as opportunity signals); improvement ideas from customer-sim missing features. | 5 | G3 ✓ | ✅ `marketplace/` + `MarketplaceApp` + `/api/genesis/marketplace` + dashboard panel | ✅ 6 tests incl. relevance gate | ✅ e2e: 3 apps listed, problem→right product, 7 demand gaps computed |
| G9 | ~~**Reality Feedback Brain**~~ (Gap 10) — ✅ DELIVERED cycle 14. Products POST REAL telemetry → ingest reacts: ERROR→QUALITY task, neg FEEDBACK→ENGINEERING, FEATURE_REQUEST→GROWTH, USAGE/RETENTION→real GrowthMetric, CONVERSION→completes acquisition CHANNEL experiment with dataSource=REAL. Every signal→memory. Reuses/extends `RealitySignal`. | 6 | G4 ✓ | ✅ `reality-feedback/` + `/api/genesis/feedback` + Mission Control panel | ✅ 7 tests | ✅ full-loop e2e: channel AWAITING_EXECUTION → product reports 37/920 → LEARNED/REAL (4%); ERROR→T-112 CRITICAL |
| G10 | ~~**SaaS layer**~~ (Gap 9) — ✅ DELIVERED cycle 15 (auth core). Hashed API-key auth + orgs + roles (OWNER/ADMIN/MEMBER/VIEWER) + audit log. Local-first enforcement: `GENESIS_AUTH_REQUIRED=1` locks protected mutations (401/403); unset = local dashboard keeps working. Guard applied to the 4 highest-risk mutation routes (approvals-decide, company-create, operator-control, feedback-ingest). Billing deferred (directive forbids autonomous payments). | 8 | — | ✅ `auth/` + `AuthUser/Organization/Membership/ApiKey/AuditLog` + `/api/genesis/auth` (bootstrap/createKey/me/audit) | ✅ 6 tests incl. gated-route 401/200 | ✅ live: 401 no-key → bootstrap OWNER → 200 → reads open (200) → VIEWER 403; every decision audited |
| G11 | **Plugin/Skill marketplace** (Gap 11) — custom agents/tools registry (AgentTemplate/CustomTool exist — extend). | 5 | — | performance tracking on existing models | template perf rows | tracked rows |
| G12 | ~~**Benchmark Arena**~~ (Gap 12) — ✅ DELIVERED cycle 13. Real-execution scored suite measuring DISCRIMINATION (rank strong>weak; refuse unsupported confidence) across EVIDENCE/VENTURE/CUSTOMER/BOARD/CHAIN + optional BUILD; autonomy score, success rate, real ms/tokens, mode; persisted for trend. Dashboard panel with RUN button. | 6 | G0 ✓ | ✅ `benchmark/` + `BenchmarkRun` + `/api/genesis/benchmark` + panel | ✅ 5 tests | ✅ live run: autonomy 100/100, 5/5 passed, HEURISTIC, 2689ms (venture 79>21, customer 82.5%>11%, board GO/NO_GO, chain all gates) |
| G13 | ~~**Dashboard UI**~~ — ✅ DELIVERED cycle 12. Two new tabs in the live dashboard: **Venture Intelligence** (pipeline runs table + AEGIS claims + venture analyses + customer sims + board decisions, honesty chips surfaced, CREATE COMPANY button) and **Mission Control** (interactive approval queue with approve/reject, long-mission progress + TICK ALL, acquisition experiment memory). | 6 | G0 ✓ | ✅ `sections/venture-intelligence.tsx` + `sections/mission-control.tsx` wired into `genesis-dashboard.tsx` | tsc/lint/build + SSR check | ✅ SSR renders both tabs; live data verified on all 7 APIs; approve-button path exercised (APR-000001 → APPROVED by dashboard-operator); TICK ALL path exercised |

## Cycle 8 — DELIVERED: G0 (Integrated Autonomous Pipeline)

- `pipeline/company.ts`: `createCompany()` — DISCOVER (OPPORTUNITY, optional focus / existing opportunityId) → VENTURE (asserts AEGIS market claim) → CUSTOMER simulation → AEGIS subject aggregate → BOARDROOM debate over the full quantified context → **build gate** (MVP build via `dispatchGoal` only on GO/CONDITIONAL; NO_GO halts honestly). `VentureRun` model records every stage; `Company` row created on approval; `VENTURE_RUN.md` artifact; memory record; API `GET/POST /api/genesis/company`.
- **Acceptance test passed (real execution)**: RUN-000011, *no idea given* → discovered OPP-000002 → venture 58 WATCH · truth 7% · customer reality 72 (60% buy of 200 SIMULATED personas) · board CONDITIONAL 59% with condition “no evidence base” surfaced → full build **7/7 done** → company `co-opp-000002`. Artifact banners HEURISTIC mode + NO_WEB_EVIDENCE + SIMULATION labels throughout.
- **Bug found & fixed by this cycle's tests**: `count()+1` id allocation in VENTURE (`VC-`), CUSTOMER (`SIM-`), OPPORTUNITY (`OPP-`) collided after any row deletion (P2002). Replaced with numeric max-scan (same fix as EX-/CLM- ids). Full suite now stable across repeated runs.
- Verified: tsc ✓, eslint ✓, **70/70 tests ×3 runs** (5 new), build compiles `/api/genesis/company`.

## Re-audit after cycle 20 — V8 COMPLETE (13/13 gaps closed)

All thirteen V8 gaps are delivered: G0 pipeline · G1 world scanner · G2 approvals · G3 demand+DNA · G4 acquisition · G5 operator · G6 arena · G7 evolution · G8 marketplace · G9 reality-feedback · G10 auth · G12 benchmark · G13 dashboard. The full autonomous loop runs and feeds back on itself: World Scanner discovers problems from Genesis's own accumulated reality → AEGIS verifies → Venture scores → Customer simulates → Boardroom debates → Build → Operate 30/60/90d → Acquire (approval-gated) → Reality feedback → Evolution → Arena competition → Demand match → Marketplace → (demand gaps + reality signals feed the World Scanner again). Every gate is honest: heuristic/simulation/computed labels throughout, no fabricated users/revenue/demand, and the benchmark scores the stack's discrimination (100/100 heuristic).

What remains is not V8 gaps but **force multipliers + hardening**, ranked: (1) **exercise the LLM path** — the single highest value-per-effort move: one `ANTHROPIC_API_KEY` turns every heuristic gate (venture/customer/board/arena teams/evolution/demand/world-scan) into real reasoning, lifts the benchmark, and activates real web scanning (G1 mode WEB_ENABLED); add a DEGRADED provider badge; (2) **guard rollout** to remaining write routes + per-org usage limits (mechanical); (3) **G11 Plugin/Skill marketplace** — perf tracking over AgentTemplate/CustomTool (evolution + arena already produce the rows); (4) real deploy hardening (the standalone-build cp step fails on the Windows junction — package/CI concern); (5) the Next-16/junction dev+preview limitation (env debt). Recommend the LLM-path cycle next — it retroactively upgrades all 20 cycles of work at once.

## Re-audit after cycle 19 (superseded)

The marketplace closes the network loop: built apps list themselves with Product DNA + demand, people with problems find products, and unserved demand segments surface as opportunity signals that can feed discovery. 12 of 13 V8 gaps closed — only **G1 World Scanner upgrade** remains, and it genuinely needs a browser/search key to add real frequency/urgency/who-suffers data to the WORLD_PROBLEM_GRAPH (without one it stays a labelled no-op, as documented). What's left is therefore either key-dependent or hardening/breadth: (1) **exercise the LLM path** — the single highest value-per-effort move: one `ANTHROPIC_API_KEY` turns every heuristic gate (venture, customer, board, arena teams, evolution, demand fit, marketplace) into real reasoning, lifts the benchmark, and lets G1 do real scanning; wire a DEGRADED provider badge; (2) **guard rollout** to remaining write routes + per-org usage limits; (3) **G11 Plugin/Skill marketplace** — perf tracking over AgentTemplate/CustomTool (the marketplace + evolution engine already produce the rows); (4) **G1 World Scanner** (once a search key exists). The autonomous venture pipeline and its feedback/evolution/competition/marketplace loops are all in place and honest.

## Re-audit after cycle 18 (superseded)

The "build → find exact users" leg is now closed: any product/opportunity gets a DNA fingerprint and a ranked demand map (who needs it, where to reach them, how urgently, how likely to adopt) with adoption grounded in the real seeded customer sim and everything else labelled heuristic/simulation. 11 of 13 V8 gaps closed (all but G1 + G8/G11). Remaining, ranked: (1) **G8 App Marketplace** — now cheap: Product DNA + DemandMatch already exist, so a marketplace is mostly a registry + match surface over built apps (net-new, buildable in heuristic mode); (2) **G11 Plugin/Skill marketplace** — perf tracking over AgentTemplate/CustomTool (evolution already writes AgentTemplate rows); (3) **G1 World Scanner upgrade** — genuinely needs a browser/search key to be non-trivial; (4) **guard rollout** + per-org usage limits (mechanical). Cross-cutting, still the highest value-per-effort: **exercise the LLM path** — one key turns every heuristic gate into real reasoning and lifts the benchmark, plus a DEGRADED provider badge.

## Re-audit after cycle 17 (superseded)

Agents now self-improve from measured performance (prompt guards, rollbacks, specialist proposals) — the improvement loop is closed and honest (no data ⇒ no action; every action carries its metric snapshot + reason; sweeps default to dry-run). The V8 gap list is nearly exhausted: 10 of 13 gaps closed (G0/G2/G4/G5/G6/G7/G9/G10/G12/G13). Remaining, ranked: (1) **G1 World Scanner upgrade** — the discovery leg is the weakest gate; genuinely needs a browser/search key to add frequency/urgency/who-suffers to the WORLD_PROBLEM_GRAPH; (2) **G3 Demand Graph + Product DNA / G8 App Marketplace** — problem↔user↔product matching (network features); (3) **guard rollout** to the remaining write routes + per-org usage limits (mechanical hardening); (4) **G11 Plugin/Skill marketplace** (extend AgentTemplate/CustomTool with perf tracking — the evolution engine already writes AgentTemplate rows, a natural feed). Cross-cutting, still the single highest value-per-effort: **exercise the LLM path** — one key turns every heuristic gate (venture/customer/board/arena teams/evolution reasoning) into real reasoning and lifts the benchmark; wire a DEGRADED provider badge into the dashboard.

## Re-audit after cycle 16 (superseded)

Genesis no longer trusts one solution: three teams compete on every mission and a judge picks the winner from real scores. A notable honest emergent behavior — in the acceptance run the arena picked BETA (best *relative* strategy) but the Boardroom then said NO_GO on it, because with a bare mission (no evidence) AEGIS truth is 0 and the board refuses to back an evidence-less venture. The layers correctly *disagree* (relative competition vs absolute evidence-grounding) rather than rubber-stamping — exactly the anti-faking intent. Remaining, ranked: (1) **G7 evolution completion** — feed the arena's winning-pattern/failed-strategy memory + AgentMetric trends into auto prompt-improvement / retire; the learning substrate now exists; (2) **guard rollout** to the remaining write routes (mechanical) + per-org usage limits; (3) **G1 World Scanner upgrade** (needs a browser key); (4) **G3 demand graph / G8 marketplace**. Cross-cutting, highest value-per-effort: **exercise the LLM path** — one key turns every heuristic gate (including the three teams' reasoning) into real reasoning and lifts the benchmark.

## Re-audit after cycle 15 (superseded)

Auth core lands: production deployments set `GENESIS_AUTH_REQUIRED=1`, run bootstrap once, and every human-authority + write endpoint is locked behind a hashed key with role checks + an audit trail; local single-operator use is unchanged. Remaining auth hardening (not blocking, ranked into the queue): the guard is applied to the 4 highest-risk mutation routes — the other write routes (v4/dispatch, orchestrator/dispatch, prompts, custom-tools, sandboxes, seed) should adopt the same `guard()` wrapper (mechanical rollout), and reads can be gated in production by extending the guard to GET handlers. Next ranked: (1) **finish the guard rollout** to the remaining mutation routes (low-risk, mechanical) + optional usage-limit counters per org; (2) **G1 World Scanner upgrade** (needs a browser key); (3) **G6 Agent Arena**; (4) **G7 evolution completion**; (5) **G3 demand graph / G8 marketplace**. Cross-cutting and highest-value-per-effort: **exercise the LLM path** — one key lifts every heuristic gate + the benchmark score at once.

## Re-audit after cycle 14 (superseded)

The outer learning loop is closed: deployed products teach Genesis (errors→fix tasks, requests→backlog, usage→real metrics, conversions→REAL experiment data), and the two honesty boundaries the Acquisition Engine deliberately left open are now crossable with genuine external data — nothing is fabricated. The full directive pipeline now runs end-to-end AND feeds back. What remains is production-hardening + breadth, not missing loop legs. Ranked: (1) **G10 SaaS/auth** — the top production blocker; every `/api/genesis/*` route is open, including the human-authority approval endpoints and the write-heavy feedback/company endpoints; (2) **G1 World Scanner upgrade** (only matters with a browser key — discovery is the weakest gate); (3) **G6 Agent Arena** (Alpha/Beta/Gamma + judge); (4) **G7 evolution completion** (auto-retire/create from AgentMetric trends); (5) **G3 demand graph / G8 marketplace**. Also outstanding: exercise the LLM path (one key lifts every heuristic gate + the benchmark score), and the Next-16/Downloads-junction dev+preview limitation (env debt, documented).

## Re-audit after cycle 13 (superseded)

Genesis now measures itself: the arena proves the intelligence stack discriminates correctly (100/100 in heuristic mode) and gives a trend line that will rise when an LLM key lands — the last *self-measurement* gap is closed. What remains are capability additions, not missing feedback. Ranked: (1) **G9 Reality Feedback** — the highest-leverage remaining: it unlocks REAL experiment data + channel execution (the two honesty boundaries the acquisition engine waits on) and closes the outer learning loop; (2) **G10 SaaS/auth** — still the top production blocker (every route open); (3) **G1 World Scanner upgrade** (needs a browser key); (4) **G6 Agent Arena** / **G7 evolution completion** / **G3 demand graph** / **G8 marketplace**. Environment debt: Next-16 dev + the preview browser can't run through the Downloads junction — UI is verified via a real-path dev server + data-path exercise, not browser pixels.

## Re-audit after cycle 12 (superseded by cycle 13)

"No hidden systems" is closed: every intelligence layer (pipeline, AEGIS, venture, customer sim, boardroom, approvals, operator, acquisition) is visible in the dashboard with its honesty labels, and the human-authority actions (approve/reject, tick) are one click. Honest boundaries unchanged: CHANNEL execution + REAL measurements wait on G9; heuristic reasoning waits on an LLM key. Environment note: Next 16 dev breaks when launched via the Downloads NTFS junction (doubled-path ENOENT) — run dev from `D:\shadow-os-dev\project` directly. Remaining ranked: (1) **G12 Benchmark Arena** — Genesis still doesn't score itself (last self-measurement gap); (2) **G9 Reality Feedback** — unlocks REAL experiment data + channel execution; (3) **G1 World Scanner upgrade** (needs a browser key); (4) **G10 SaaS layer** (auth is still the top production blocker); (5) **G6 Agent Arena / G7 evolution completion**.
