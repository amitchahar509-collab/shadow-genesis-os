import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SHADOW GENESIS OS — Autonomous AI Company Operating System",
  description:
    "Autonomous AI company execution engine. DISCOVER → VALIDATE → DESIGN → BUILD → TEST → DEPLOY → MEASURE → IMPROVE → REPEAT. Transform a human vision into a real working product.",
  keywords: [
    "AI OS",
    "Autonomous Agents",
    "AI Company",
    "Genesis",
    "Agent Orchestration",
    "Operating System",
  ],
  authors: [{ name: "Shadow Genesis" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
